-- ============================================================
-- TREK MOTORS CUBA - Migración: Agregar columna currency a products
-- Ejecutar en el SQL Editor de Supabase
-- ============================================================

ALTER TABLE public.products
ADD COLUMN IF NOT EXISTS currency TEXT DEFAULT 'USD' CHECK (currency IN ('USD', 'MLC', 'CUP', 'EUR'));

-- Actualizar productos existentes sin moneda a USD
UPDATE public.products SET currency = 'USD' WHERE currency IS NULL;
